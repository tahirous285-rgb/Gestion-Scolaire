import { apiGet, apiPost, apiPut, apiDelete } from "./apiClient"; export const getNotes=({id_evaluation,id_inscription}={})=>apiGet(`/notes/?${new URLSearchParams({...(id_evaluation?{id_evaluation}:{}),...(id_inscription?{id_inscription}:{})})}`); export const createNote=d=>apiPost("/notes/",d); export const updateNote=(id,d)=>apiPut(`/notes/${id}`,d); export const deleteNote=id=>apiDelete(`/notes/${id}`);